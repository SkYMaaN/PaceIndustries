@page "/account/logout"

<PageTitle>Sign out</PageTitle>

@code {
    [Inject] private AuthenticationStateProvider _authStateProvider { get; set; }

    protected override void OnInitialized()
    {
        (_authStateProvider as CustomAuthenticationStateProvider).Logout();
    }
}
